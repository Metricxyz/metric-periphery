#!/bin/sh
# Runs before each local git commit: forge fmt --check, then forge test.
# Enable once per clone: git config --local core.hooksPath .githooks
# Skip when needed: git commit --no-verify

set -e
cd "$(git rev-parse --show-toplevel)" || exit 1

forge fmt --check
forge test
